import pandas as pd
import matplotlib.pyplot as plt

def plot_booking_curves(lt_df: pd.DataFrame, title: str = "") -> None:
    """
    単一月・単一曜日などの LT_DATA をもとにブッキングカーブを描画するための土台関数（仮）。
    実際の仕様は今後の壁打ちで詰めていく。
    """
    raise NotImplementedError("plot_booking_curves の仕様は今後検討します。")
